# Roop Ultimate — Claude Code

**All agent rules for this repository are in [`AGENTS.md`](AGENTS.md).** Read it in full
before doing anything else and follow it exactly; nothing is repeated here.

Claude-specific notes:

- Claude Code also loads the parent directory's `<PINOKIO_HOME>/CLAUDE.md` automatically
  (the Pinokio launcher guide). `AGENTS.md` Part 2 is the same guide plus this project's
  two hardware profiles; where they differ, `AGENTS.md` is the one checked into this repo.
- Read `<MEDIA_DIR>/RECODE_STATUS.md` (outside the repo) newest section first at the start
  of every session, as `AGENTS.md` Part 1 says.
